import React from "react";
import "./FormulaView.css";

const FormulaView = () => (
  <div className="formula-container">
    <h3 className="formula-title">How to Calculate Area?</h3>
    <p className="formula-text">
      The area of a triangle is calculated using:
      <br />
      <b>Area = ½ × base × height</b>
    </p>
  </div>
);

export default FormulaView;
